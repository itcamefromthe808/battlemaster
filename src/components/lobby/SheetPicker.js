import React, { Component } from 'react';

class SheetPicker extends Component {
  render() {
    return (
        <div className="sheet-picker">Sheets Functionality Not Built (yet)</div>
    );
  }
}

export default SheetPicker;
